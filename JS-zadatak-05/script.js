const populacija = 3.8e6;
const prosjek = 33e6;

if (populacija > prosjek) {
    console.log(`Populacija Hrvatske (${populacija}) je veća od proseka stanovnika EU (${prosjek}).`);
} else {
    console.log(`Populacija Hrvatske (${populacija}) je manja od proseka stanovnika EU (${prosjek}).`);
}